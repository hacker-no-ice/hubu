name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Test workspace
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings

      - name: Run tests
        run: cargo test --workspace --locked

      - name: Build release binaries with locked dependencies
        run: cargo build --release --locked --bin hubu --bin hubu-server

      - name: Check default credentials cannot be committed
        run: ./scripts/check-default-credential-ignore.sh

      - name: Run core integration flow
        run: ./scripts/integration-core-flow.sh

      - name: Build docs
        run: cargo doc --workspace --no-deps --locked

  msrv:
    name: Check MSRV (Rust 1.78.0)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install MSRV toolchain
        run: rustup toolchain install 1.78.0 --profile minimal

      - name: Check workspace with MSRV
        run: cargo +1.78.0 check --workspace --all-targets --all-features --locked
